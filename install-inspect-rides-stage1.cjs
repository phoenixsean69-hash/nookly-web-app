const fs = require("fs");
const path = require("path");
const { spawnSync } = require("child_process");

const root = process.cwd();
const targetPath = path.join(
  root,
  "functions",
  "rides-driver-api",
  "src",
  "organization-handler.js",
);

if (!fs.existsSync(targetPath)) {
  throw new Error(`Target file was not found: ${targetPath}`);
}

const original = fs.readFileSync(targetPath, "utf8");

if (original.includes("listOrganizationRideInspections")) {
  console.log("Inspect Rides Stage 1 is already installed.");
  process.exit(0);
}

const replacements = [
  {
    name: "ride inspection tables",
    anchor: "  rides: env(\n    \"APPWRITE_RIDES_TABLE_ID\",\n    env(\"EXPO_PUBLIC_APPWRITE_RIDES_COLLECTION_ID\", \"rides\"),\n  ),\n};",
    replacement: "  rides: env(\n    \"APPWRITE_RIDES_TABLE_ID\",\n    env(\"EXPO_PUBLIC_APPWRITE_RIDES_COLLECTION_ID\", \"rides\"),\n  ),\n  bookings: env(\n    \"APPWRITE_RIDE_BOOKINGS_TABLE_ID\",\n    env(\"EXPO_PUBLIC_APPWRITE_RIDE_BOOKINGS_COLLECTION_ID\", \"ride_bookings\"),\n  ),\n  locations: env(\n    \"APPWRITE_RIDE_LOCATIONS_TABLE_ID\",\n    env(\"EXPO_PUBLIC_APPWRITE_RIDE_LOCATIONS_COLLECTION_ID\", \"ride_locations\"),\n  ),\n  incidents: env(\n    \"APPWRITE_RIDE_INCIDENTS_TABLE_ID\",\n    env(\"EXPO_PUBLIC_APPWRITE_RIDE_INCIDENTS_COLLECTION_ID\", \"ride_incidents\"),\n  ),\n  events: env(\n    \"APPWRITE_RIDE_EVENTS_TABLE_ID\",\n    env(\"EXPO_PUBLIC_APPWRITE_RIDE_EVENTS_COLLECTION_ID\", \"ride_events\"),\n  ),\n  tripCore: env(\n    \"APPWRITE_RIDE_TRIP_CORE_TABLE_ID\",\n    \"ride_trip_core\",\n  ),\n  tripWaypoints: env(\n    \"APPWRITE_RIDE_TRIP_WAYPOINTS_TABLE_ID\",\n    \"ride_trip_waypoints\",\n  ),\n  expectedRoutePoints: env(\n    \"APPWRITE_RIDE_EXPECTED_ROUTE_POINTS_TABLE_ID\",\n    \"ride_expected_route_points\",\n  ),\n};",
  },
  {
    name: "ride inspection constants",
    anchor: "const ACTIVE_RIDE_STATUSES = new Set([\"boarding\", \"active\", \"delayed\"]);",
    replacement: "const ACTIVE_RIDE_STATUSES = new Set([\"boarding\", \"active\", \"delayed\"]);\n\nconst INSPECTABLE_RIDE_STATUSES = new Set([\"boarding\", \"active\", \"delayed\"]);\nconst CONFIRMED_BOOKING_STATUSES = new Set([\n  \"confirmed\",\n  \"checked_in\",\n  \"boarded\",\n  \"active\",\n]);\nconst OPEN_INCIDENT_STATUSES = new Set([\"open\", \"investigating\"]);\nconst DEFAULT_ROUTE_CORRIDOR_METERS = 300;\nconst LOCATION_WARNING_MINUTES = 2;\nconst LOCATION_CRITICAL_MINUTES = 5;\nconst STOPPED_RIDE_WARNING_MINUTES = 10;",
  },
  {
    name: "ride inspection service",
    anchor: "const approveApplication = async ({",
    replacement: "\nconst numberOrNull = (value) => {\n  if (value === null || value === undefined || value === \"\") {\n    return null;\n  }\n\n  const parsed = Number(value);\n  return Number.isFinite(parsed) ? parsed : null;\n};\n\nconst timestampOrNull = (value) => {\n  if (!value) return null;\n\n  const timestamp = new Date(value).getTime();\n  return Number.isFinite(timestamp) ? timestamp : null;\n};\n\nconst getRowOrNull = async (tablesDB, tableId, rowId) => {\n  const normalizedRowId = String(rowId || \"\").trim();\n\n  if (!normalizedRowId) return null;\n\n  try {\n    return await tablesDB.getRow({\n      databaseId: DATABASE_ID,\n      tableId,\n      rowId: normalizedRowId,\n    });\n  } catch {\n    return null;\n  }\n};\n\nconst sortByNumericField = (rows, field) =>\n  [...rows].sort(\n    (left, right) =>\n      Number(left?.[field] ?? 0) - Number(right?.[field] ?? 0),\n  );\n\nconst sortLocationsNewestFirst = (locations) =>\n  [...locations].sort((left, right) => {\n    const leftSequence = numberOrNull(left?.sequence);\n    const rightSequence = numberOrNull(right?.sequence);\n\n    if (leftSequence !== null && rightSequence !== null) {\n      return rightSequence - leftSequence;\n    }\n\n    const leftTime =\n      timestampOrNull(\n        left?.recordedAt ||\n          left?.createdAt ||\n          left?.$createdAt,\n      ) ?? 0;\n    const rightTime =\n      timestampOrNull(\n        right?.recordedAt ||\n          right?.createdAt ||\n          right?.$createdAt,\n      ) ?? 0;\n\n    return rightTime - leftTime;\n  });\n\nconst toRadians = (degrees) => (degrees * Math.PI) / 180;\n\nconst haversineMeters = (\n  startLatitude,\n  startLongitude,\n  endLatitude,\n  endLongitude,\n) => {\n  const earthRadiusMeters = 6371000;\n  const latitudeDelta = toRadians(endLatitude - startLatitude);\n  const longitudeDelta = toRadians(endLongitude - startLongitude);\n\n  const value =\n    Math.sin(latitudeDelta / 2) ** 2 +\n    Math.cos(toRadians(startLatitude)) *\n      Math.cos(toRadians(endLatitude)) *\n      Math.sin(longitudeDelta / 2) ** 2;\n\n  return (\n    earthRadiusMeters *\n    2 *\n    Math.atan2(Math.sqrt(value), Math.sqrt(1 - value))\n  );\n};\n\nconst distancePointToSegmentMeters = (point, start, end) => {\n  const originLatitude = point.latitude;\n  const latitudeScale = 111320;\n  const longitudeScale =\n    111320 * Math.cos(toRadians(originLatitude));\n\n  const toLocalPoint = (value) => ({\n    x: (value.longitude - point.longitude) * longitudeScale,\n    y: (value.latitude - point.latitude) * latitudeScale,\n  });\n\n  const segmentStart = toLocalPoint(start);\n  const segmentEnd = toLocalPoint(end);\n  const segmentX = segmentEnd.x - segmentStart.x;\n  const segmentY = segmentEnd.y - segmentStart.y;\n  const segmentLengthSquared =\n    segmentX * segmentX + segmentY * segmentY;\n\n  if (segmentLengthSquared <= 0) {\n    return Math.hypot(segmentStart.x, segmentStart.y);\n  }\n\n  const projection = Math.max(\n    0,\n    Math.min(\n      1,\n      -(\n        segmentStart.x * segmentX +\n        segmentStart.y * segmentY\n      ) / segmentLengthSquared,\n    ),\n  );\n\n  const nearestX = segmentStart.x + projection * segmentX;\n  const nearestY = segmentStart.y + projection * segmentY;\n\n  return Math.hypot(nearestX, nearestY);\n};\n\nconst getRouteDeviationMeters = (\n  currentLocation,\n  expectedRoutePoints,\n) => {\n  if (!currentLocation || expectedRoutePoints.length === 0) {\n    return null;\n  }\n\n  if (expectedRoutePoints.length === 1) {\n    const point = expectedRoutePoints[0];\n\n    return haversineMeters(\n      currentLocation.latitude,\n      currentLocation.longitude,\n      point.latitude,\n      point.longitude,\n    );\n  }\n\n  let minimumDistance = Number.POSITIVE_INFINITY;\n\n  for (let index = 0; index < expectedRoutePoints.length - 1; index += 1) {\n    const distance = distancePointToSegmentMeters(\n      currentLocation,\n      expectedRoutePoints[index],\n      expectedRoutePoints[index + 1],\n    );\n\n    minimumDistance = Math.min(minimumDistance, distance);\n  }\n\n  return Number.isFinite(minimumDistance)\n    ? Math.round(minimumDistance)\n    : null;\n};\n\nconst getConsecutiveStoppedMinutes = (locations) => {\n  const newestFirst = sortLocationsNewestFirst(locations);\n  const latest = newestFirst[0];\n\n  if (!latest) return 0;\n\n  const latestSpeed = numberOrNull(latest.speedKph);\n\n  if (latestSpeed === null || latestSpeed > 3) {\n    return 0;\n  }\n\n  const latestTime = timestampOrNull(\n    latest.recordedAt || latest.createdAt || latest.$createdAt,\n  );\n\n  if (latestTime === null) return 0;\n\n  let earliestStoppedTime = latestTime;\n\n  for (const location of newestFirst) {\n    const speed = numberOrNull(location.speedKph);\n    const time = timestampOrNull(\n      location.recordedAt ||\n        location.createdAt ||\n        location.$createdAt,\n    );\n\n    if (speed === null || speed > 3 || time === null) {\n      break;\n    }\n\n    earliestStoppedTime = Math.min(earliestStoppedTime, time);\n  }\n\n  return Math.max(\n    0,\n    Math.floor((latestTime - earliestStoppedTime) / 60000),\n  );\n};\n\nconst buildSurveillanceAssessment = ({\n  ride,\n  driver,\n  currentLocation,\n  locations,\n  tripCore,\n  expectedRoutePoints,\n  incidents,\n}) => {\n  const alerts = [];\n\n  const addAlert = (code, severity, title, message, data = {}) => {\n    alerts.push({\n      code,\n      severity,\n      title,\n      message,\n      data,\n    });\n  };\n\n  const currentStatus = normalize(ride.status);\n  const now = Date.now();\n\n  if (!currentLocation) {\n    addAlert(\n      \"location_unavailable\",\n      \"critical\",\n      \"Driver location is unavailable\",\n      \"The organization cannot currently see where this ride is.\",\n    );\n  } else {\n    const locationTimestamp = timestampOrNull(currentLocation.recordedAt);\n    const locationAgeMinutes =\n      locationTimestamp === null\n        ? null\n        : Math.max(0, (now - locationTimestamp) / 60000);\n\n    if (\n      locationAgeMinutes !== null &&\n      locationAgeMinutes >= LOCATION_CRITICAL_MINUTES\n    ) {\n      addAlert(\n        \"location_very_stale\",\n        \"critical\",\n        \"Driver location has not updated\",\n        `The last location update was ${Math.floor(\n          locationAgeMinutes,\n        )} minutes ago.`,\n        { locationAgeMinutes: Math.round(locationAgeMinutes * 10) / 10 },\n      );\n    } else if (\n      locationAgeMinutes !== null &&\n      locationAgeMinutes >= LOCATION_WARNING_MINUTES\n    ) {\n      addAlert(\n        \"location_stale\",\n        \"warning\",\n        \"Driver location is becoming outdated\",\n        `The last location update was ${Math.floor(\n          locationAgeMinutes,\n        )} minutes ago.`,\n        { locationAgeMinutes: Math.round(locationAgeMinutes * 10) / 10 },\n      );\n    }\n\n    const speedKph = numberOrNull(currentLocation.speedKph);\n\n    if (speedKph !== null && speedKph > 120) {\n      addAlert(\n        \"speed_critical\",\n        \"critical\",\n        \"Very high vehicle speed detected\",\n        `The latest reported speed is ${Math.round(speedKph)} km/h.`,\n        { speedKph },\n      );\n    } else if (speedKph !== null && speedKph > 90) {\n      addAlert(\n        \"speed_warning\",\n        \"warning\",\n        \"High vehicle speed detected\",\n        `The latest reported speed is ${Math.round(speedKph)} km/h.`,\n        { speedKph },\n      );\n    }\n\n    const accuracyMeters = numberOrNull(\n      currentLocation.accuracyMeters,\n    );\n\n    if (accuracyMeters !== null && accuracyMeters > 100) {\n      addAlert(\n        \"poor_gps_accuracy\",\n        \"warning\",\n        \"Location accuracy is poor\",\n        `The driver's reported position may be inaccurate by about ${Math.round(\n          accuracyMeters,\n        )} metres.`,\n        { accuracyMeters },\n      );\n    }\n  }\n\n  if (\n    [\"active\", \"delayed\"].includes(currentStatus) &&\n    driver?.isOnline === false\n  ) {\n    addAlert(\n      \"driver_offline\",\n      \"warning\",\n      \"Driver appears to be offline\",\n      \"The driver's account is no longer reporting as online during this ride.\",\n    );\n  }\n\n  const routeCorridorMeters = Math.max(\n    50,\n    numberOrNull(tripCore?.routeCorridorMeters) ??\n      DEFAULT_ROUTE_CORRIDOR_METERS,\n  );\n  const routeDeviationMeters = getRouteDeviationMeters(\n    currentLocation,\n    expectedRoutePoints,\n  );\n\n  if (routeDeviationMeters !== null) {\n    if (routeDeviationMeters > routeCorridorMeters * 2) {\n      addAlert(\n        \"route_deviation_critical\",\n        \"critical\",\n        \"Driver is far outside the expected route\",\n        `The driver is approximately ${routeDeviationMeters} metres away from the expected route.`,\n        { routeDeviationMeters, routeCorridorMeters },\n      );\n    } else if (routeDeviationMeters > routeCorridorMeters) {\n      addAlert(\n        \"route_deviation\",\n        \"warning\",\n        \"Driver has moved outside the expected route\",\n        `The driver is approximately ${routeDeviationMeters} metres away from the expected route.`,\n        { routeDeviationMeters, routeCorridorMeters },\n      );\n    }\n  } else {\n    addAlert(\n      \"expected_route_unavailable\",\n      \"warning\",\n      \"Expected route is unavailable\",\n      \"Route-deviation checking cannot run until expected route points are available.\",\n    );\n  }\n\n  const stoppedMinutes = getConsecutiveStoppedMinutes(locations);\n\n  if (\n    currentStatus === \"active\" &&\n    stoppedMinutes >= STOPPED_RIDE_WARNING_MINUTES\n  ) {\n    addAlert(\n      \"stopped_too_long\",\n      \"warning\",\n      \"Vehicle has been stopped for a long time\",\n      `The latest location history indicates that the vehicle remained stopped for about ${stoppedMinutes} minutes.`,\n      { stoppedMinutes },\n    );\n  }\n\n  const expectedArrivalTime = timestampOrNull(\n    ride.estimatedArrivalTime,\n  );\n\n  if (\n    [\"active\", \"delayed\"].includes(currentStatus) &&\n    expectedArrivalTime !== null &&\n    now > expectedArrivalTime + 10 * 60000\n  ) {\n    const overdueMinutes = Math.floor(\n      (now - expectedArrivalTime) / 60000,\n    );\n\n    addAlert(\n      \"ride_overdue\",\n      overdueMinutes >= 30 ? \"critical\" : \"warning\",\n      \"Ride is beyond its expected arrival time\",\n      `This ride is approximately ${overdueMinutes} minutes overdue.`,\n      { overdueMinutes },\n    );\n  }\n\n  const openIncidents = incidents.filter((incident) =>\n    OPEN_INCIDENT_STATUSES.has(normalize(incident.status)),\n  );\n\n  if (openIncidents.length > 0) {\n    const hasHighPriorityIncident = openIncidents.some((incident) =>\n      [\"high\", \"critical\", \"emergency\"].includes(\n        normalize(incident.priority),\n      ),\n    );\n\n    addAlert(\n      \"open_incidents\",\n      hasHighPriorityIncident ? \"critical\" : \"warning\",\n      \"Open ride incident requires attention\",\n      `${openIncidents.length} open incident${\n        openIncidents.length === 1 ? \"\" : \"s\"\n      } ${\n        openIncidents.length === 1 ? \"is\" : \"are\"\n      } linked to this ride.`,\n      { openIncidentCount: openIncidents.length },\n    );\n  }\n\n  const severityRank = {\n    clear: 0,\n    warning: 1,\n    critical: 2,\n  };\n\n  const overallStatus = alerts.reduce(\n    (current, alert) =>\n      severityRank[alert.severity] > severityRank[current]\n        ? alert.severity\n        : current,\n    \"clear\",\n  );\n\n  return {\n    overallStatus,\n    checkedAt: nowIso(),\n    routeCorridorMeters,\n    routeDeviationMeters,\n    stoppedMinutes,\n    alertCount: alerts.length,\n    alerts,\n  };\n};\n\nconst normalizeLocation = (ride, latestLocation) => {\n  const latitude =\n    numberOrNull(ride.currentLatitude) ??\n    numberOrNull(latestLocation?.latitude);\n  const longitude =\n    numberOrNull(ride.currentLongitude) ??\n    numberOrNull(latestLocation?.longitude);\n\n  if (latitude === null || longitude === null) {\n    return null;\n  }\n\n  return {\n    latitude,\n    longitude,\n    heading:\n      numberOrNull(ride.currentHeading) ??\n      numberOrNull(latestLocation?.heading),\n    speedKph:\n      numberOrNull(ride.currentSpeedKph) ??\n      numberOrNull(latestLocation?.speedKph),\n    accuracyMeters:\n      numberOrNull(ride.currentAccuracyMeters) ??\n      numberOrNull(latestLocation?.accuracyMeters),\n    recordedAt:\n      ride.lastLocationAt ||\n      latestLocation?.recordedAt ||\n      latestLocation?.createdAt ||\n      latestLocation?.$createdAt ||\n      null,\n  };\n};\n\nconst sanitizeBookingForInspection = (booking) => ({\n  $id: booking.$id,\n  studentId: booking.studentId,\n  studentName: booking.studentName || \"B.U.S.E student\",\n  studentPhone: booking.studentPhone || undefined,\n  status: booking.status,\n  seatCount: Number(\n    booking.seatCount ?? booking.passengerCount ?? 1,\n  ),\n  passengerCount: Number(\n    booking.passengerCount ?? booking.seatCount ?? 1,\n  ),\n  pickupAddress: booking.pickupAddress || undefined,\n  pickupLatitude: numberOrNull(booking.pickupLatitude),\n  pickupLongitude: numberOrNull(booking.pickupLongitude),\n  destinationAddress: booking.destinationAddress || undefined,\n  destinationLatitude: numberOrNull(\n    booking.destinationLatitude,\n  ),\n  destinationLongitude: numberOrNull(\n    booking.destinationLongitude,\n  ),\n  bookingReference: booking.bookingReference || undefined,\n  bookedAt:\n    booking.bookedAt ||\n    booking.createdAt ||\n    booking.$createdAt ||\n    undefined,\n});\n\nconst buildRideInspection = async (\n  tablesDB,\n  organizationId,\n  rideId,\n  { locationLimit = 100, includeEvents = true } = {},\n) => {\n  const ride = await getRowOrNull(tablesDB, TABLES.rides, rideId);\n\n  if (!ride) {\n    throw statusError(404, \"Ride was not found.\");\n  }\n\n  if (\n    String(ride.organizationId || \"\") !== String(organizationId)\n  ) {\n    throw statusError(\n      403,\n      \"This ride belongs to another organization.\",\n    );\n  }\n\n  if (!INSPECTABLE_RIDE_STATUSES.has(normalize(ride.status))) {\n    throw statusError(\n      409,\n      \"Only boarding, active, or delayed rides can be inspected.\",\n    );\n  }\n\n  const [\n    bookings,\n    locations,\n    incidents,\n    events,\n    tripCoreRows,\n    waypointRows,\n    expectedRouteRows,\n    driver,\n    vehicle,\n  ] = await Promise.all([\n    listAllRows(\n      tablesDB,\n      TABLES.bookings,\n      [Query.equal(\"rideId\", ride.$id)],\n      100,\n    ),\n    listAllRows(\n      tablesDB,\n      TABLES.locations,\n      [Query.equal(\"rideId\", ride.$id)],\n      locationLimit,\n    ),\n    listAllRows(\n      tablesDB,\n      TABLES.incidents,\n      [Query.equal(\"rideId\", ride.$id)],\n      100,\n    ),\n    includeEvents\n      ? listAllRows(\n          tablesDB,\n          TABLES.events,\n          [Query.equal(\"rideId\", ride.$id)],\n          200,\n        )\n      : Promise.resolve([]),\n    listAllRows(\n      tablesDB,\n      TABLES.tripCore,\n      [Query.equal(\"rideId\", ride.$id)],\n      5,\n    ),\n    listAllRows(\n      tablesDB,\n      TABLES.tripWaypoints,\n      [Query.equal(\"rideId\", ride.$id)],\n      50,\n    ),\n    listAllRows(\n      tablesDB,\n      TABLES.expectedRoutePoints,\n      [Query.equal(\"rideId\", ride.$id)],\n      1000,\n    ),\n    getRowOrNull(tablesDB, TABLES.drivers, ride.driverId),\n    getRowOrNull(tablesDB, TABLES.vehicles, ride.vehicleId),\n  ]);\n\n  const confirmedBookings = bookings.filter((booking) => {\n    const bookingOrganizationId = String(\n      booking.organizationId || \"\",\n    ).trim();\n\n    return (\n      CONFIRMED_BOOKING_STATUSES.has(normalize(booking.status)) &&\n      (!bookingOrganizationId ||\n        bookingOrganizationId === String(organizationId))\n    );\n  });\n\n  if (confirmedBookings.length === 0) {\n    throw statusError(\n      404,\n      \"No confirmed B.U.S.E student booking is linked to this active ride.\",\n    );\n  }\n\n  const sortedLocations = sortLocationsNewestFirst(locations);\n  const latestLocation = sortedLocations[0] ?? null;\n  const currentLocation = normalizeLocation(ride, latestLocation);\n  const tripCore = tripCoreRows[0] ?? null;\n\n  const waypoints = sortByNumericField(\n    waypointRows,\n    \"stopOrder\",\n  ).map((waypoint) => ({\n    $id: waypoint.$id,\n    type: waypoint.waypointType,\n    address: waypoint.address,\n    latitude: numberOrNull(waypoint.latitude),\n    longitude: numberOrNull(waypoint.longitude),\n    stopOrder: Number(waypoint.stopOrder || 0),\n  }));\n\n  const expectedRoutePoints = sortByNumericField(\n    expectedRouteRows,\n    \"sequence\",\n  )\n    .map((point) => ({\n      latitude: numberOrNull(point.latitude),\n      longitude: numberOrNull(point.longitude),\n      sequence: Number(point.sequence || 0),\n    }))\n    .filter(\n      (point) =>\n        point.latitude !== null && point.longitude !== null,\n    );\n\n  const surveillance = buildSurveillanceAssessment({\n    ride,\n    driver,\n    currentLocation,\n    locations: sortedLocations,\n    tripCore,\n    expectedRoutePoints,\n    incidents,\n  });\n\n  const students = confirmedBookings.map(\n    sanitizeBookingForInspection,\n  );\n\n  const totalPassengers = students.reduce(\n    (total, booking) =>\n      total + Math.max(1, Number(booking.passengerCount || 1)),\n    0,\n  );\n\n  return {\n    ride: {\n      $id: ride.$id,\n      externalReference: ride.externalReference || undefined,\n      organizationId: ride.organizationId,\n      status: ride.status,\n      departureTime: ride.departureTime || undefined,\n      estimatedArrivalTime:\n        ride.estimatedArrivalTime || undefined,\n      startedAt: ride.startedAt || undefined,\n      fare: numberOrNull(ride.fare),\n      currency: ride.currency || \"USD\",\n      totalSeats: numberOrNull(ride.totalSeats),\n      bookedSeats: numberOrNull(ride.bookedSeats),\n      availableSeats: numberOrNull(ride.availableSeats),\n      currentLocation,\n    },\n    driver: driver\n      ? {\n          $id: driver.$id,\n          name: driver.name || ride.driverName || \"Driver\",\n          phone: driver.phone || undefined,\n          email: driver.email || undefined,\n          avatar: driver.avatar || ride.driverAvatar || undefined,\n          isOnline: driver.isOnline === true,\n          lastSeenAt: driver.lastSeenAt || undefined,\n          rating: numberOrNull(driver.rating),\n          completedTrips: numberOrNull(\n            driver.completedTrips,\n          ),\n        }\n      : {\n          $id: ride.driverId,\n          name: ride.driverName || \"Driver\",\n          isOnline: false,\n        },\n    vehicle: vehicle\n      ? {\n          $id: vehicle.$id,\n          registrationNumber:\n            vehicle.registrationNumber ||\n            ride.vehicleRegistration ||\n            \"Not provided\",\n          make: vehicle.make || ride.vehicleMake || undefined,\n          model: vehicle.model || ride.vehicleModel || undefined,\n          color: vehicle.color || ride.vehicleColor || undefined,\n          capacity:\n            numberOrNull(vehicle.passengerCapacity) ??\n            numberOrNull(vehicle.capacity) ??\n            numberOrNull(ride.vehicleCapacity),\n          status: vehicle.status || undefined,\n        }\n      : {\n          $id: ride.vehicleId,\n          registrationNumber:\n            ride.vehicleRegistration || \"Not provided\",\n          make: ride.vehicleMake || undefined,\n          model: ride.vehicleModel || undefined,\n          color: ride.vehicleColor || undefined,\n          capacity: numberOrNull(ride.vehicleCapacity),\n        },\n    students,\n    totalPassengers,\n    route: {\n      expectedDistanceKm: numberOrNull(\n        tripCore?.expectedDistanceKm,\n      ),\n      expectedDurationMinutes: numberOrNull(\n        tripCore?.expectedDurationMinutes,\n      ),\n      routeCorridorMeters:\n        numberOrNull(tripCore?.routeCorridorMeters) ??\n        DEFAULT_ROUTE_CORRIDOR_METERS,\n      waypoints,\n      expectedRoutePoints,\n    },\n    locationHistory: sortedLocations.map((location) => ({\n      $id: location.$id,\n      latitude: numberOrNull(location.latitude),\n      longitude: numberOrNull(location.longitude),\n      heading: numberOrNull(location.heading),\n      speedKph: numberOrNull(location.speedKph),\n      accuracyMeters: numberOrNull(location.accuracyMeters),\n      recordedAt:\n        location.recordedAt ||\n        location.createdAt ||\n        location.$createdAt ||\n        undefined,\n      sequence: numberOrNull(location.sequence),\n    })),\n    incidents: [...incidents].sort((left, right) => {\n      const leftTime =\n        timestampOrNull(\n          left.createdAt || left.$createdAt,\n        ) ?? 0;\n      const rightTime =\n        timestampOrNull(\n          right.createdAt || right.$createdAt,\n        ) ?? 0;\n      return rightTime - leftTime;\n    }),\n    events: [...events].sort((left, right) => {\n      const leftTime =\n        timestampOrNull(\n          left.createdAt || left.$createdAt,\n        ) ?? 0;\n      const rightTime =\n        timestampOrNull(\n          right.createdAt || right.$createdAt,\n        ) ?? 0;\n      return rightTime - leftTime;\n    }),\n    surveillance,\n    refreshedAt: nowIso(),\n  };\n};\n\nconst listOrganizationRideInspections = async (\n  tablesDB,\n  organizationId,\n) => {\n  const rides = await listAllRows(\n    tablesDB,\n    TABLES.rides,\n    [Query.equal(\"organizationId\", organizationId)],\n    500,\n  );\n\n  const inspectableRides = rides\n    .filter((ride) =>\n      INSPECTABLE_RIDE_STATUSES.has(normalize(ride.status)),\n    )\n    .slice(0, 100);\n\n  const inspections = await Promise.all(\n    inspectableRides.map((ride) =>\n      buildRideInspection(\n        tablesDB,\n        organizationId,\n        ride.$id,\n        {\n          locationLimit: 100,\n          includeEvents: false,\n        },\n      ).catch((caughtError) => {\n        if (\n          caughtError?.statusCode === 404 ||\n          caughtError?.statusCode === 409\n        ) {\n          return null;\n        }\n\n        throw caughtError;\n      }),\n    ),\n  );\n\n  const visibleInspections = inspections\n    .filter(Boolean)\n    .sort((left, right) => {\n      const severityRank = {\n        critical: 0,\n        warning: 1,\n        clear: 2,\n      };\n      const leftRank =\n        severityRank[left.surveillance.overallStatus] ?? 3;\n      const rightRank =\n        severityRank[right.surveillance.overallStatus] ?? 3;\n\n      if (leftRank !== rightRank) return leftRank - rightRank;\n\n      const leftTime =\n        timestampOrNull(left.ride.departureTime) ?? 0;\n      const rightTime =\n        timestampOrNull(right.ride.departureTime) ?? 0;\n\n      return rightTime - leftTime;\n    });\n\n  return {\n    generatedAt: nowIso(),\n    counts: {\n      total: visibleInspections.length,\n      critical: visibleInspections.filter(\n        (inspection) =>\n          inspection.surveillance.overallStatus === \"critical\",\n      ).length,\n      warning: visibleInspections.filter(\n        (inspection) =>\n          inspection.surveillance.overallStatus === \"warning\",\n      ).length,\n      clear: visibleInspections.filter(\n        (inspection) =>\n          inspection.surveillance.overallStatus === \"clear\",\n      ).length,\n    },\n    rides: visibleInspections,\n  };\n};\n\nconst approveApplication = async ({",
  },
  {
    name: "ride inspection routes",
    anchor: "    if (method === \"GET\" && path === \"/organization/drivers\") {",
    replacement: "    if (\n      method === \"GET\" &&\n      path === \"/organization/rides/inspection\"\n    ) {\n      return ok(\n        res,\n        await listOrganizationRideInspections(\n          tablesDB,\n          organization.$id,\n        ),\n      );\n    }\n\n    if (\n      method === \"GET\" &&\n      parts.length === 4 &&\n      parts[0] === \"organization\" &&\n      parts[1] === \"rides\" &&\n      parts[3] === \"inspection\"\n    ) {\n      return ok(\n        res,\n        await buildRideInspection(\n          tablesDB,\n          organization.$id,\n          parts[2],\n          {\n            locationLimit: 500,\n            includeEvents: true,\n          },\n        ),\n      );\n    }\n\n    if (method === \"GET\" && path === \"/organization/drivers\") {",
  },
];

let updated = original;

for (const item of replacements) {
  const occurrences = updated.split(item.anchor).length - 1;

  if (occurrences !== 1) {
    throw new Error(
      `Could not safely install ${item.name}. Expected exactly one anchor, found ${occurrences}.`,
    );
  }

  updated = updated.replace(item.anchor, item.replacement);
}

const requiredMarkers = [
  "listOrganizationRideInspections",
  "/organization/rides/inspection",
  "route_deviation_critical",
  "No confirmed B.U.S.E student booking",
  "ride_expected_route_points",
];

for (const marker of requiredMarkers) {
  if (!updated.includes(marker)) {
    throw new Error(`Generated source is missing marker: ${marker}`);
  }
}

const backupPath = path.join(
  path.dirname(targetPath),
  "organization-handler.before-inspect-rides.js",
);

if (!fs.existsSync(backupPath)) {
  fs.writeFileSync(backupPath, original, "utf8");
}

fs.writeFileSync(targetPath, updated, "utf8");

const syntaxCheck = spawnSync(
  process.execPath,
  ["--check", targetPath],
  {
    cwd: root,
    encoding: "utf8",
  },
);

if (syntaxCheck.status !== 0) {
  fs.writeFileSync(targetPath, original, "utf8");

  throw new Error(
    [
      "The updated Function failed JavaScript syntax validation.",
      syntaxCheck.stdout,
      syntaxCheck.stderr,
      "The original file was restored.",
    ]
      .filter(Boolean)
      .join("\n"),
  );
}

console.log("Installed Inspect Rides Stage 1.");
console.log("Updated: functions/rides-driver-api/src/organization-handler.js");
console.log("Backup: functions/rides-driver-api/src/organization-handler.before-inspect-rides.js");
console.log("Added: GET /organization/rides/inspection");
console.log("Added: GET /organization/rides/:rideId/inspection");
console.log("Added organization scoping, confirmed-student checks, route deviation, stale GPS, speed, stop, overdue, offline, GPS accuracy and incident surveillance.");
console.log("JavaScript syntax check passed.");
console.log("");
console.log("Do not deploy yet. Paste this output so we can verify Stage 1 before Appwrite deployment.");
